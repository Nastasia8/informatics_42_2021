#include <iostream>
#include <cstdlib>
#include <ctime>
using namespace std;


int main()
{
    srand(time(NULL));
    int minim = 10000;
    int k;
    cout << "Print number of length = " << endl;
    cin >> k;
    int *p = new int [k];
    for (int i=0; i<k; i++)
   {
    p[i] = rand() % 100;
    cout<<p[i]<<endl;
  }
  for (int i=0; i<k;i++)
  {
    if(p[i]<minim)
    {
      minim=p[i];
    }
}
	cout<<"minimal number is "<<minim<<endl;
}
