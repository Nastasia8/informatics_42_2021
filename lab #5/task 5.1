#include <iostream>
using namespace std;

void place(int *x, int *y, int *z); 

int main()
{
    int x, y, z;
    cout<<"Type x, y, z"<<endl;
    //ввод переменных x, y, z
    cin>> x;
    cin>> y;
    cin>> z;
    place(&x, &y, &z); //вызов функции с передачей аргументов по ссылке
    cout<<" "<<endl;
    cout<<"Now it's changed"<<endl;
    cout<<x<<" "<<y<<" "<<z<<endl;
    return 0;
}

void place(int *x, int *y, int *z) //объявление функции
{
    int o = *x;
    *x=*x-55;
    *y=*y-4;
    *z=*z+100;
    // работа над переменными
}
